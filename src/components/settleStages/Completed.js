import React from "react";
import Header from "../Header";

const Completed = () => {
	return (
		<div className="completed">
		<Header />
		<p>Completed</p>
		</div>
	)
};

export default Completed;
